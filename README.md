# Spring AOP Advices

Uncomment the `throw new RuntimeException("Simulated payment processing error");` line in the `processPayment` method of the `PaymentService` class to simulate an exception during payment processing.